package com.ab.collection.concurrent;

/**
 * @author Arpit Bhardwaj
 *
 * ConcurrentSkipListSet implements NavigableSet<E>, interfaces.
 * ConcurrentSkipListSet does not allow null elements.
 * All the elements are sorted based on natural ordering or by the Comparator passed during it’s construction time.
 *
 * ConcurrentSkipListMap implements ConcurrentNavigableMap interface.
 * ConcurrentSkipListMap  does not allow null elements.
 * All the elements are sorted based on natural ordering or by the Comparator passed during it’s construction time.
 */
public class ConcurrentSkipListSetAndMapDemo {
}
